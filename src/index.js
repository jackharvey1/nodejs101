// run a server, feed in env vars (process), output uptime at startup (os), doing some shit with a file (fs , path), probably write data from an API to a file? (http), promisify the fs functions (util), struggle with file size and deal with it with stream and child_process
const main = require('./main');

main();
